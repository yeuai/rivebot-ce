import { DominatePipe } from './dominate.pipe';

describe('DominatePipe', () => {
  it('create an instance', () => {
    const pipe = new DominatePipe();
    expect(pipe).toBeTruthy();
  });

  it('create a dominate number', () => {
    const pipe = new DominatePipe();
    expect(pipe).toBeTruthy();
  });
});
